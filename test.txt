1) Значение sep по ум. -

2) Значение end по ум. -

3) Что делает специальный символ \ ? МОжно использовать как деление, а так сам по себе ничего

4) Что делает специальный символ \n ? переносит текст после /n в следующюю строку

5) Что делает специальный символ \t ? Пробел буд-то TABом

6) Что делает математический оператор % ? Деление с остатком

7) Что делает математический оператор // ? Деление

8) Что делает математический оператор ** ? Деление в степени типо (5**3 = 5*5*5)

9) Что возвращает abs(-2) ? 2

10) Что возвращает pow(2, 4) ? 16( число в степень)

11) Что возвратит round(5 / 2) ? 2

# второй аргумент в функции round означает до скольки символов после запятой будет выполняться округление, например round(10 / 3, 2) вернёт 3.33

12) Зачем нужна директива del ? Для удаления

11) Создайте переменную "age" и присвойте ей значение вашего возраста. Выведите ее значение. Затем используйте директиву "del" для удаления этой переменной, и попробуйте вывести значение снова. Что произошло? Ошибка произошла